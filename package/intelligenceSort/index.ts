/**
 * Sort an array of numbers with intelligence sort.
 * Intelligence sort is a sorting algorithm that assumes that the input values are already sorted by divine intervention, or it was intended that way by a higher power.
 * Deus Vult.
 * O(1) time complexity.
 * O(1) space complexity.
 *
 * @param {number[]} values
 */
export const intelligenceSort = (
	values: number[],
) => {
	return values;
};
